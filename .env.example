# Copy to .env and fill in your keys (shared by research + summarize servers)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override agent config file
# TOKENOPS_CONFIG=src/tokenops/config/default.yaml

# Search backend: duckduckgo (default, free via ddgs) or corpus (offline fixture)
# SEARCH_BACKEND=duckduckgo
# SEARCH_TIMEOUT=20
# SEARCH_REGION=us-en
